# YAHEALTHY — רשימת אבטחה ופרטיות

> **למי זה:** מי שעובד על הקוד של YAHEALTHY ומי שמחליט מתי הוא עולה לאוויר.
> **עודכן:** 20/09/2026 · **הבעלים:** VLAD (ארכיטקטורה) · **נמדד מהקוד, לא מהתיעוד.**

---

## איך קוראים את זה

לכל שורה יש **ראיה** — קובץ ושורה, פלט של כלי, או בדיקה שרצה. **אין כאן סימון "בוצע" בלי ראיה.** סעיף בלי ראיה הוא סעיף פתוח, גם אם נכתב קוד שנראה כאילו הוא מטפל בו.

| סימון | פירוש |
|---|---|
| `[x]` | נסגר, **עם ראיה** |
| `[~]` | חלקי — מה שחסר כתוב בשורה |
| `[ ]` | פתוח |
| `[-]` | **אין משטח כזה במערכת.** לא חסר — לא קיים |
| `[!]` | **לא הכרעה של מפתח.** בעל העסק או עו"ד |

---

## 1. זהות וסשנים

- [x] **סיסמאות מגובבות** — bcrypt, 10 סבבים · `utils/auth.js:33`
- [x] **סיסמה לא חוזרת מה-API** — כל תשובות המשתמש מחזירות `id/email/name` בלבד
- [x] **טוקן גישה ניתן לביטול** — `token_version` נחתם ונבדק מול השורה · `utils/auth.js` · ADR-008
- [x] **התנתקות אמיתית בצד השרת** — `POST /api/auth/logout` · `index.js:269`
- [x] **איפוס סיסמה מנתק כל סשן קודם** — `index.js:385`
- [x] **שינוי סיסמה מנתק כל סשן קודם** — ומנפיק טוקן חדש לקורא · `index.js:310`
- [x] **טוקן איפוס חד-פעמי** — נושא את גרסת הטוקן שהייתה בהנפקה; האיפוס מעלה אותה
- [x] **לטוקן יש סוג, ורק `access` פותח סשן** — `utils/auth.js`. **לפני התיקון טוקן איפוס התקבל כסשן מלא**
- [x] **אלגוריתם JWT נעול** — `algorithms: ['HS256']`, בחתימה ובאימות
- [x] **נכשל סגור** — לא ניתן לאמת מול הדאטה → 503, לא מעבר על סמך חתימה
- [x] **אין enumeration** — התחברות ואיפוס מחזירים תשובות אחידות · `index.js:216,300`
- [x] **`JWT_SECRET` חובה בייצור** — השרת מסרב לעלות בלעדיו · `utils/auth.js:10`
- [x] **מינימום 10 תווים לסיסמה** — בהגדרה, לא בבדיקה, כדי לא לנעול חשבונות קיימים
- [ ] **אימות כתובת מייל** — אין. נפתח יחד עם ספק המייל
- [ ] **התנתקות פר-מכשיר** — היום התנתקות מנתקת הכול. דורש טבלת סשנים
- [ ] **נעילה/האטה מתקדמת אחרי כשלונות** — יש rate limit, אין נעילת חשבון
- [ ] **MFA** — לא רלוונטי לשלב. להכריע כשיהיה פאנל ניהול
- [-] **Refresh tokens** — אין. כל עוד אין סשן קצר-טווח, אין מה לרענן
- [-] **Cookies · CSRF** — הטוקן נשלח ב-`Authorization`, לא בעוגייה

**ראיה מרכזית:** `npm run test:tokens` → **21 עברו, 0 נכשלו.** כל מקרה בודק שהשרת **מסרב**.

---

## 2. הרשאות

- [x] **כל endpoint מאומת** — למעט health, ready, docs ושני ה-webhooks
- [x] **בעלות נאכפת בשאילתה, לא ב-`if`** — `utils/database.js:313` ו-`:375`: `.eq('id',…).eq('user_id',…)`
- [x] **אין IDOR** — נבדק בשכבת הדאטה, לא רק בנתיב. נכון גם במצב זיכרון
- [x] **`/api/whatsapp/pending` מאומת** — `index.js:80`. מחזיר תוכן הודעות של לקוחות
- [x] **הרשאה לפי מסלול (entitlement)** — `middleware/requireEntitlement.js`. נכשל סגור: מנוי שאי אפשר לאמת אינו מנוי
- [x] **מסלול השף חסום בשרת** — מנוי פעיל **וגם** שבוע מתוכנן · `routes/chef.js`. לא ב-prompt ולא בכפתור
- [-] **RBAC / תפקידים** — אין תפקידים במערכת
- [-] **בידוד tenants** — אין ארגונים
- [-] **פאנל ניהול** — לא קיים

---

## 3. API

- [x] **ולידציית סכימה** — zod על הנתיבים שמקבלים גוף
- [x] **הגבלת גודל payload** — 100KB, ברירת המחדל של `express.json()`
- [x] **Rate limiting** — 500/15דק' כללי, 50/15דק' על `/api/auth` · `middleware/rateLimit.js`
- [x] **`trust proxy` מוגדר** — `index.js:57`. בלעדיו כל המשתמשים היו דלי אחד
- [x] **שגיאות לא מדליפות פנימיות** — `safeErrorDetails` משתיק בייצור · `index.js:60`
- [x] **CORS לפי allowlist** — `CORS_ORIGINS`; ייצור מסרב לעלות בלי הגדרה
- [x] **Security headers** — helmet
- [~] **CSP** — **כבוי בכוונה.** `public/login.html` ו-`index.html` מכילים סקריפטים inline ו-38 מאפייני `style` inline. הפעלה דורשת nonces לדפים האלה קודם
- [x] **נתיב הסליקה מחוץ ל-rate limiter** — `index.js`. כל הקריאות מ-PayPlus מגיעות מ-IP אחד; ההגנה שלו היא החתימה, שלא נחלשת תחת עומס
- [ ] **HTTPS / HSTS בפועל** — תלוי בפלטפורמת הפריסה

---

## 3א. Webhooks

- [x] **חתימת PayPlus מאומתת** — HMAC-SHA256 על גוף הבקשה, מפתח הסוד, השוואה בזמן קבוע · `utils/payplus.js`
- [x] **בלי סוד — מסרב** — לא "עובר בינתיים". `PAYPLUS_SECRET_KEY` חסר = 401
- [x] **אידמפוטנטיות בדאטה, לא ב-`if`** — `payment_events.page_request_uid` הוא מפתח ראשי; מנוי פעיל כפול נחסם ב-partial unique index
- [x] **גוף גולמי נשמר לנתיב הזה בלבד** — `express.json({ verify })`, לא לכל בקשה
- [x] **webhook וואטסאפ מוגן בסוד בנתיב** — `WHATSAPP_WEBHOOK_SECRET` · `routes/whatsapp.js`
- [x] **מסירה כפולה של וואטסאפ לא יוצרת שורה שנייה** — מזהה ההודעה הוא המפתח הראשי
- [ ] **timestamp / חלון זמן על ה-callback** — אין. הגנת replay נשענת כרגע על מפתח ייחודי בלבד
- [ ] **סבב סודות** — לא הוגדר תהליך

---

## 4. קלט והזרקות

- [x] **אין SQL גולמי** — הכול דרך `@supabase/supabase-js`
- [x] **אין הזרקת NoSQL** — `mongoose` הוסר; הוא גם נשא CVE פעיל
- [-] **SSRF** — אין ולו קריאת HTTP יוצאת אחת מהשרת
- [-] **Command injection** — אין `exec`/`spawn` בנתיב בקשה
- [-] **Path traversal בהעלאות** — אין העלאות קבצים
- [ ] **XSS בדפים שהשרת מגיש** — לא נבדק. קשור לסעיף ה-CSP

---

## 5. סודות

- [x] **אין סודות ב-git** — נבדק גם בהיסטוריה
- [x] **`.gitignore` מכסה `.env*`** — עם חריג ל-`.env.example`
- [x] **אין סוד בקוד לקוח** — ה-frontend מחזיק רק `VITE_API_URL`
- [x] **הפרדה בין מפתח שרת למפתח לקוח** — `SUPABASE_KEY` מול `SUPABASE_PUBLISHABLE_KEY`
- [ ] **סבב מפתחות** — `JWT_SECRET` הנוכחי הוא ערך פיתוח. **להחליף באקראי לפני ייצור**
- [ ] **סודות PayPlus** — טרם הוגדרו

---

## 6. דאטה ופרטיות

- [x] **RLS מופעל על כל הטבלאות** — `migrations/001`, `002`
- [x] **הדאטה לא נעלמת בשקט** — השרת מסרב לעלות בלי Supabase; זיכרון רק עם דגל מפורש · `utils/database.js:18`
- [x] **הודעות וואטסאפ לא נענות אוטומטית** — 36 דגלי בריאות מסמנים `escalated` · `routes/whatsapp.js`
- [x] **דגל בריאותי לא נכתב ללוג** — `routes/whatsapp.js` מתעד מזהה, לא תוכן
- [x] **התוכנית של הלקוח שורדת אתחול** — `meal_plans` בטבלה, לא במערך בזיכרון · `migrations/004`
- [x] **מחיקת חשבון** — `DELETE /api/users/me`, דורשת סיסמה מחדש. 11 טבלאות נמחקות ב-cascade
- [x] **מה שלא נמחק נאמר במפורש** — הודעות וואטסאפ מפתוחות לפי מספר טלפון ולא לפי חשבון; התשובה אומרת זאת ללקוח
- [ ] **מדיניות שמירה (retention)** — אין. `whatsapp_messages.raw` שומר הודעות מלאות לנצח
- [ ] **ייצוא נתונים אישיים** — אין
- [ ] **מפת דאטה** — `PRIVACY_DATA_MAP.md` טרם נכתב
- [ ] **יומן ביקורת** — אין רישום לכניסות, כשלונות ושינויי סיסמה
- [!] **גיל מינימום 10** — `index.js:436`. **מוצר בתשלום עם יעד משקל שמקבל בני 10 — זו החלטה שלך, לא שלי.**
- [!] **מדיניות פרטיות · תקנון · זכות ביטול · הצהרת אי-ייעוץ רפואי** — מ1–מ4 ב-`compliance-register.md`. **עו"ד.**

---

## 7. שרשרת אספקה

- [x] **`npm audit` נקי** — 0 פגיעויות (היו 10, מתוכן 7 גבוהות)
- [x] **כלי פיתוח לא נשלחים לייצור** — `nodemon` הועבר ל-`devDependencies`
- [x] **`package-lock.json` בגרסה** — כן
- [ ] **סריקת תלויות אוטומטית ב-CI** — אין
- [ ] **`deploy.yml` מדווח הצלחה גם בכישלון** — `continue-on-error` + `if: success()`. **בעלים: DevOps**

---

## 8. בדיקות

**`npm run test:all` — 74 מקרים, 3 חבילות, כולן ירוקות.**

- [x] **מחזור חיי טוקנים** — 29 מקרים · `tests/token-lifecycle.test.js`
- [x] **בעלות על משאבים (IDOR)** — 22 מקרים מול שני חשבונות · `tests/meal-plans.test.js`
- [x] **mass assignment** — בקשה שמנסה לשכתב `user_id` דרך גוף הבקשה
- [x] **חתימת webhook** — 23 מקרים: בלי חתימה, חתימה שגויה, חתימה על תוכן אחר, חתימה במפתח אחר · `tests/payments-and-chef.test.js`
- [x] **אידמפוטנטיות** — אותו callback פעמיים
- [x] **חסימת הרשאה** — בקשת שף בלי מנוי ובלי שבוע מתוכנן
- [x] **מחיקת חשבון** — כולל שחשבון חדש על אותה כתובת לא יורש דאטה
- [ ] **rate limiting** · **CORS** — אין בדיקות
- [ ] **רגרסיה כללית** — `test-system.sh` קיים, לא אומת בסבב הזה

---

## מה חוסם עלייה לאוויר, נכון לעכשיו

**הכול כאן מדיד, ואף אחד מהם אינו בעיה בקוד.**

| # | מה | מי |
|---|---|---|
| 1 | **להריץ את מיגרציות 003–006** ב-Supabase. עד אז התנתקות, תוכניות, מנויים ובקשות שף יחזירו 500 | את/ה, 5 דקות |
| 2 | **להחליף `JWT_SECRET`** בערך אקראי. הנוכחי הוא ערך פיתוח | את/ה |
| 3 | **לבחור ספק מייל** ולמלא את `deliver()` ב-`utils/mailer.js`. עד אז אין מייל בייצור — השרת מסרב לשלוח ולא מעמיד פנים | את/ה |
| 4 | **חשבון PayPlus** — מפתחות, `PAYPLUS_PAYMENT_PAGE_UID`, מחירים, **ו-sandbox קודם** | את/ה |
| 5 | **מדיניות שמירה** ל-`whatsapp_messages` | הכרעה + מימוש קצר |
| 6 | **גיל מינימום 10** — החלטה עסקית-בריאותית | את/ה |
| 7 | **מ1–מ4** — תקנון, פרטיות, זכות ביטול, אי-ייעוץ רפואי | עו"ד |

**הסטטוס הכולל: NOT READY.** לא ציון — שבעה תנאים, כל אחד מדיד, אף אחד מהם לא באג פתוח.
